My project is set up.

<!-- Summary -->
Scenario A costs about $1.66 per month, while Scenario B costs about $2,517.23 per month using the AWS Pricing Calculator. The large difference was surprising, especially because the p3.2xlarge GPU instance alone costs about $2,233.80 per month when running 24/7. While exploring the calculator, I found that Savings Plans and Reserved Instances can reduce costs compared with On-Demand pricing. Overall, the comparison shows that GPU instances can be worth the cost for workloads such as machine learning training that actually need GPU acceleration, but they are far too expensive for lightweight workloads that can run on a small CPU instance. 

